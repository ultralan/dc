batch_size: 2
num_workers: 0
max_epochs: 1
limit_train_steps: 5
learning_rate: 0.0003
weight_decay: 0.01
gradient_clip_val: 1.0
val_batch_size: ${train.batch_size}
run_validation: false
validate_every_n_steps: 0
metric_for_best_checkpoint: val/loss
metric_mode: min
secondary_metric_for_best_checkpoint: null
secondary_metric_mode: ${train.metric_mode}
early_stopping_patience: 0
early_stopping_min_delta: 0.0
resume_from: null
force_resume: false
pin_memory: false
persistent_workers: false
prefetch_factor: null
log_every_n_steps: 1
save_every_n_steps: 0
output_root: ${hydra:runtime.cwd}/runs
loss_slot_activity_pos_weight: 1.0
loss_slot_activity_neg_weight: 1.0
loss_future_slot_activity_pos_weight: 1.0
loss_future_slot_activity_neg_weight: 1.0
loss_future_slot_deactivate_weight: 1.0
loss_slot_count_consistency_weight: 0.0
loss_future_slot_count_consistency_weight: 0.0
loss_future_slot_count_transition_weight: 1.0
loss_current_heat_kl_weight: 0.5
loss_future_heat_kl_weight: 0.75
loss_future_slot_delta_weight: 1.0
loss_slot_heat_consistency_weight: 0.5
probe_validation: false
probe_scenarios: []
probe_animation_steps: 24
probe_source_audio: null
